<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Reviews | YourScent.co</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body { background: #f7f8fb; }
    .page { max-width: 900px; margin: 32px auto; padding: 0 16px 48px; }
    .card { margin-bottom: 12px; }
    .rating { color: #f39c12; font-weight: 700; }
  </style>
</head>
<body>
  <div class="page">
    <h2>My Reviews</h2>
    <% if (!reviews || reviews.length === 0) { %>
      <p>You haven't left any reviews yet.</p>
    <% } else { %>
      <% reviews.forEach(r => { %>
        <div class="card p-3">
          <div class="d-flex align-items-center">
            <img src="/images/<%= r.image %>" alt="<%= r.perfumeName %>" style="width:64px;height:64px;object-fit:cover;border-radius:8px;margin-right:12px;" />
            <div>
              <div><strong><%= r.perfumeName %></strong></div>
              <div class="rating">Rating: <%= r.rating %> / 5</div>
            </div>
            <div class="ms-auto text-muted"><%= new Date(r.created_at).toLocaleString() %></div>
          </div>
          <% if (r.comment) { %>
            <div class="mt-2"><%= r.comment %></div>
          <% } %>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>
