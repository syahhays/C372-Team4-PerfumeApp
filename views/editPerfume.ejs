<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Perfume | YourScent.co</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background: #f7f8fb;
      color: #1a1a1a;
    }
    .page {
      max-width: 900px;
      margin: 32px auto;
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 12px 32px rgba(0, 0, 0, 0.08);
      padding: 24px;
    }
    .form-label {
      font-weight: 600;
    }
    .btn-primary-soft {
      background: #a000ff;
      border: none;
    }
    .btn-primary-soft:hover {
      background: #8900d8;
    }
  </style>
</head>
<body>
  <%- include('partials/adminNavbar') %>

  <div class="page">
    <h2 class="mb-4">Edit Perfume</h2>
    <form action="/editPerfume/<%= perfume.perfumeId %>" method="POST">
      <div class="row g-3">
        <div class="col-md-6">
          <label class="form-label" for="perfumeName">Name</label>
          <input class="form-control" id="perfumeName" name="perfumeName" type="text" value="<%= perfume.perfumeName %>" required />
        </div>
        <div class="col-md-6">
          <label class="form-label" for="gender">Gender</label>
          <input class="form-control" id="gender" name="gender" type="text" value="<%= perfume.gender %>" readonly />
          <small class="text-muted">Current gender from database</small>
        </div>
        <div class="col-12">
          <label class="form-label" for="perfumeDescription">Description</label>
          <textarea class="form-control" id="perfumeDescription" name="perfumeDescription" rows="3" required><%= perfume.perfumeDescription %></textarea>
        </div>
        <div class="col-12">
          <label class="form-label" for="perfumeNotes">Notes (comma separated)</label>
          <textarea class="form-control" id="perfumeNotes" name="perfumeNotes" rows="2"><%= perfume.perfumeNotes %></textarea>
        </div>
        <div class="col-md-4">
          <label class="form-label" for="quantity">Quantity</label>
          <input class="form-control" id="quantity" name="quantity" type="number" min="0" value="<%= perfume.quantity %>" required />
        </div>
        <div class="col-md-4">
          <label class="form-label" for="price">Price</label>
          <input class="form-control" id="price" name="price" type="number" step="0.01" min="0" value="<%= perfume.price %>" required />
        </div>
        <div class="col-md-4">
          <label class="form-label" for="image">Image filename/path</label>
          <input class="form-control" id="image" name="image" type="text" value="<%= perfume.image %>" />
          <small class="text-muted">e.g., image.jpg (served from /images)</small>
        </div>
        <div class="col-md-8 d-flex flex-column">
          <span class="form-label">Current Image</span>
          <% if (perfume.image) { %>
            <img src="/images/<%= perfume.image %>" alt="<%= perfume.perfumeName %>" class="img-fluid rounded border" style="max-height: 280px; object-fit: contain;" />
          <% } else { %>
            <p class="text-muted">No image available.</p>
          <% } %>
        </div>
      </div>

      <div class="d-flex justify-content-between align-items-center mt-4">
        <a href="/inventory" class="btn btn-outline-secondary">Cancel</a>
        <button class="btn btn-primary-soft text-white px-4" type="submit">Save Changes</button>
      </div>
    </form>
  </div>
</body>
</html>
