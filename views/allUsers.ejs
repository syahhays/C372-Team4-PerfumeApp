<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <title>YourScent.co - Users (Admin)</title>

    <style>
        body {
            background: #f7f8fb;
            color: #1a1a1a;
        }

        .card {
            transition: all 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.3);
        }

        .role-badge {
            position: absolute;
            top: 12px;
            left: 12px;
            background: #f2e9ff;
            color: #7c00ff;
            border-radius: 999px;
            padding: 6px 12px;
            font-weight: 600;
            font-size: 0.85rem;
        }

        .status-pill {
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 0.85rem;
            font-weight: 600;
            display: inline-block;
        }

        .status-active {
            background: #e6fffa;
            color: #047857;
        }

        .status-banned {
            background: #ffecec;
            color: #c1121f;
        }

        .btn-outline-soft {
            border: 1px solid #e3e3e3;
            color: #1a1a1a;
            background: #fff;
        }

        .btn-outline-soft:hover {
            border-color: #7c00ff;
            color: #7c00ff;
            background: #f7edff;
        }

        .btn-edit-soft {
            background: #e9f5ff;
            color: #0c5aa6;
            border: 1px solid #c8e4ff;
        }

        .btn-edit-soft:hover {
            background: #d6ecff;
        }

        .btn-danger-soft {
            background: #ffecec;
            color: #c1121f;
            border: 1px solid #ffd4d4;
        }

        .btn-danger-soft:hover {
            background: #ffc9c9;
        }

        .action-row {
            display: flex;
            gap: 8px;
            margin-top: 12px;
        }
    </style>
</head>
<body>

<%- include('partials/adminNavbar') %>

<div class="container mt-5">
    <div class="text-center mb-4">
        <h2 class="mb-0">User Management (Admin)</h2>
        <small class="text-muted">View and manage all registered users</small>
    </div>
    <form class="row g-2 mb-4" method="GET" action="/allUsers">
        <div class="col-md-6">
            <input
                type="text"
                name="search"
                class="form-control"
                placeholder="Search by username..."
                value="<%= search %>"
            >
        </div>
        <div class="col-md-3">
            <select name="sort" class="form-select">
                <option value="desc" <%= sort === 'desc' ? 'selected' : '' %>>
                    Newest First
                </option>
                <option value="asc" <%= sort === 'asc' ? 'selected' : '' %>>
                    Oldest First
                </option>
            </select>
        </div>
        <div class="col-md-3">
            <button type="submit" class="btn btn-primary-soft text-white w-100">
                Search & Sort
            </button>
        </div>
    </form>
    <div class="row g-4">
        <% users.forEach(user => { %>
            <div class="col-sm-6 col-md-4 col-lg-3">
                <div class="card h-100 shadow-sm position-relative">

                    <span class="role-badge"><%= user.role %></span>

                    <div class="card-body text-center">
                        <h5 class="card-title mb-1"><%= user.username %></h5>
                        <p class="text-muted mb-1"><%= user.email %></p>
                        <p class="mb-2"><%= user.contact || 'No contact' %></p>

                        <% if (user.banned) { %>
                            <span class="status-pill status-banned">Banned</span>
                        <% } else { %>
                            <span class="status-pill status-active">Active</span>
                        <% } %>

                        <p class="text-muted mt-2 mb-0" style="font-size: 0.8rem;">
                            Joined: <%= new Date(user.createdAt).toLocaleDateString() %>
                        </p>

                        <div class="action-row">
                            <a href="/admin/users/<%= user.userId %>" class="btn btn-outline-soft w-100">
                                View
                            </a>
                        </div>

                        <div class="action-row">
                            <a href="/admin/users/edit/<%= user.userId %>" class="btn btn-edit-soft w-50">
                                Edit
                            </a>

                            <% if (!user.banned) { %>
                                <a href="/admin/users/ban/<%= user.userId %>" class="btn btn-danger-soft w-50">
                                    Ban
                                </a>
                            <% } else { %>
                                <a href="/admin/users/unban/<%= user.userId %>" class="btn btn-outline-soft w-50">
                                    Unban
                                </a>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</div>

</body>
</html>
